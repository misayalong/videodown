# 验证记录

## v0.2.5 抖音推荐流 MSE 地址处理（2026-09-19）

- 用户确认替换 v0.2.4 的手动补参数实现。现在从当前视频元素的最近 React 节点沿组件链读取作品 ID 匹配的 `xgplayerConfig`，复用网站的 `preProcessUrl` 处理所选 MP4 或 DASH 音视频地址；不依赖 `currentSrc` 是否为 HTTPS。
- 地址处理仍限制在既有 HTTPS 媒体域名，结果必须与输入具有相同来源和路径。播放器异常不会将带签名的 URL 透传到菜单；配置尚未就绪时提示开始播放并重试。未增加权限、依赖、下载方式或跨平台改动。
- 在修改实现前，以实际 MAIN → 后台 → offscreen 构建新增两个 `blob:` 回归，两者稳定返回 prime CDN HTTP 403；修改后抖音及完整文件链路的 34 项定向测试通过。覆盖 HTTPS/blob 两类播放、两个 prime 节点、最终 Blob 字节一致、DASH 双轨地址处理、切换作品不串用地址、拒绝域名/路径替换及异常信息脱敏。
- `npm test` 完成 TypeScript 检查、六入口构建，14 个测试文件 / 108 项全部通过（13:49）；`git diff --check` 通过。项目、源码 manifest 与构建 manifest 均为 0.2.5。
- 用户已重载，刷新推荐流后确认真实菜单显示 v0.2.5，没有修改页面媒体输入。HTTPS 作品 `7682388958164672613` / 540P HEVC 拉取到系统保存对话框；切换到普通 MSE/blob 作品 `7685295594633547035` 后，菜单标题、ID 和画质均正确，720P 拉取到 99%，未再出现原先的 403。广告拒绝解析、直播无入口仍正常。
- 独立详情页 `7682754874433359114` 此次也使用 blob 播放，v0.2.5 正确解析 720P/576P；未在本轮重复下载详情样例。
- **本版推荐流最终文件验收通过**：用户完成 Chrome 保存后，在下载目录找到两份正式 v0.2.5 文件。`ffprobe` 确认各有完整视频与 AAC 双声道音轨；`ffmpeg -nostdin -nostats -xerror -i <文件> -map 0:v:0 -map 0:a:0 -af volumedetect -f null -` 全片解码均退出 0，音频均非全静音。此前 99% 时尚未完成系统保存，不以进度代替文件验收。
- 当前完成标准已验证：HTTPS/blob 两类普通推荐作品可以保存含音频的 MP4；切换作品后菜单使用新作品；相关自动化回归通过。真实下载样本为完整 MP4，DASH 双轨地址处理仍仅由自动化覆盖；未扩展权限、生产依赖、批量下载、直播或图集支持。

| v0.2.5 正式推荐流下载 | 视频轨 | 音频轨 | 文件时长 | 大小 | 完整解码 |
| --- | --- | --- | --- | --- | --- |
| HTTPS / `7682388958164672613` / 540P | HEVC 960×540 | AAC，双声道 | 870.566667 秒 | 43,426,183 字节 | 退出 0 |
| MSE/blob / `7685295594633547035` / 720P | H.264 1280×720 | AAC，双声道 | 179.141995 秒 | 40,630,164 字节 | 退出 0 |

两份文件均保留在用户下载目录，未纳入仓库。音频峰值分别为 -0.1 dB 与 -6.2 dB；未改写或转码下载文件。

## v0.2.4 抖音推荐流 403 修复（2026-09-19）

- 用户反馈推荐流下载出现 `HTTP 403，v11-web-prime.douyinvod.com`，独立详情页可下载。v0.2.3 的真实下载验证集中在详情页，推荐流此前只验证了目标切换和菜单，未覆盖最终下载。
- 在推荐流作品 `7682754874433359114` / 720P 上连续两次复现同一错误。原始 `bitRateList.playAddr` 使用 prime CDN；对应的播放器 `video.currentSrc` 在相同地址上追加了 `webid` 与 `fid`。
- 禁用请求缓存后逐项对照：原始地址的普通 GET、Range GET 均为 403；真实播放地址的 1 KiB Range 请求为 206，移除 `webid` 后变为 403，单独移除 `fid` 仍为 206。没有输出参数值、Cookie 或带签名的完整地址。
- 修复限定在抖音 MAIN 解析路径：从当前目标的视频元素取得播放地址，给缺少 `webid` 的 `*-web-prime.douyinvod.com` 候选补齐该参数。保留原有参数与签名查询串编码，不复制 `fid`；只信任原有媒体域名规则允许的播放地址。未增加权限、依赖或备用下载流程。
- `npx vitest run tests/file-download.test.ts -t '推荐流'` 在旧构建中有 2 项失败，均由实际 MAIN → 后台 → offscreen 链路产生相同 HTTP 403；修复后两项通过。最终 `npm test` 完成 TypeScript 检查、六入口构建，13 个文件 / 96 项测试通过（11:10），`git diff --check` 通过；项目、源码 manifest 与构建 manifest 的版本均确认为 0.2.4。
- 为验证真实 CDN 根因，只临时修正当前页一次解析输入，解析后立即还原；原 v0.2.3 扩展随即成功保存推荐流样例。最终文件 11,070,413 字节，H.264 720×1280 + AAC，114.939002 秒；`ffmpeg -v error -i <文件> -f null -` 完整解码退出码为 0。此为真实链路的受控对照，不能替代正式 v0.2.4 重载验收。
- 同一作品的 576P MP4 补齐参数后，1 KiB Range 请求也为 206。其 DASH 视频和独立音轨仍返回 403，增加 `fid` 或换成平台提供的另一个 prime 节点也未恢复；本次实际菜单选择完整 MP4。分轨分支的参数传递经过自动化测试，不能据此声称 prime CDN 的分轨下载已真实通过。
- **v0.2.4 正式重载复验：部分通过，尚未完整修复。** 刷新推荐流后确认菜单版本为 0.2.4；作品 `7667114766448751763` 使用 `v11-web-prime.douyinvod.com` 及普通 HTTPS 播放地址，576P 成功下载 84,946,632 字节，H.264 794×576 + AAC，491.588005 秒，完整解码退出码为 0。页面未做临时修改。
- 切换时直播无下载入口，广告 `7654212110704117353` 明确拒绝解析。下一普通作品 `7681882033257254179` 的菜单标题、媒体 ID、画质均正确，但其播放器使用 `blob:` 地址，720P 下载再次返回 `HTTP 403，v26-web-prime.douyinvod.com`；`currentSrc` 不含可复用的 `webid`。这证明仅补齐 HTTPS 播放地址参数不能覆盖正常推荐流中的 MSE 播放模式。
- 最小替代路径已完成受控对照：从当前视频对应组件、且 `awemeInfo.awemeId` 匹配的 `xgplayerConfig` 取得网站现有的 `preProcessUrl`，处理后媒体地址的域名、路径、原参数不变，新增 `webid` 与 `fid`。接口形状参考 [XGPlayer 官方实现](https://github.com/bytedance/xgplayer/blob/main/packages/xgplayer/src/player.js#L2197)。仅临时替换一次解析输入后立即还原，现有扩展即成功下载同一 blob 样例：42,535,060 字节，H.264 720×1280 + AAC，248.151995 秒，完整解码退出码为 0。该结果不能当作未修改的 v0.2.4 已修复。
- v0.2.4 独立详情页回归：`7682754874433359114` / 576P 最终文件 10,182,488 字节，H.264 576×1024 + AAC，114.939002 秒，完整解码退出码为 0。
- v0.2.4 验收后提出改用当前作品播放器的 `preProcessUrl`，替换手动复制 `webid`。用户已确认，实施及复验状态见上方 v0.2.5 记录；v0.2.4 的 96 项测试结果不代表 MSE 场景已经覆盖。

## v0.2.3 抖音/B 站接入（2026-09-19）

- `npm test`：TypeScript 检查和六入口构建通过，13 个测试文件、90 项测试通过（v0.2.3，09:13）。`git diff --check` 通过。
- 新增覆盖：B 站当前分 P 与重试、实际媒体轨筛选、高帧率画质、AVC/HEVC/AV1 选择、抖音 MP4/DASH 配对、水印下载源标注、推荐流和弹层目标切换、伪造 CDN 拒绝、来源规则的扩展/域名边界及注册失败恢复。
- 使用用户已登录的 Chrome 只读核查了抖音推荐流和详情播放器的媒体字段；样例同时提供 MP4 与 DASH，2160P/1440P 为 HEVC，1080P 及以下有 AVC。
- B 站真实播放页内的 `/x/player/playurl` 请求返回成功，具有多个实际画质及 AAC 音轨；验证所用接口参数与本次实现一致。
- 两站各一条视频 CDN 做了 1 KiB Range 请求对照：页面来源请求为 HTTP 206，去掉 Referer 后为 HTTP 403。此实验解释了新增规则的必要性，但不等同于扩展自身发起的请求已经通过。
- 用户截图及真实页面日志确认 v0.2.0 已启用且成功注入，此前仅凭未看到浮层判断需要重载的依据不足。
- 真实 B 站菜单复现「当前分 P 不存在」：接口分 P 数据正常，但 MAIN 构建暴露的全局函数 `f` 被网站同名函数覆盖。新增经典脚本构建测试在旧构建中复现相同错误；将两个新增 MAIN 入口改为 IIFE 后通过，同时确认内部变量不再泄露到网站全局。
- v0.2.1 真实 B 站 `BV1n54y1t7hU`：菜单正确显示当前分 P，P1 下载后 ffprobe 确认 H.264 640×360 + AAC，8.4265 秒、655,322 字节；切 P2 后菜单标题、时长、媒体 ID 和文件名均切换，下载文件为 H.264 358×738 + AAC，15.1624 秒、752,150 字节。实际启动 P2 合并后点击取消，菜单恢复可选，重开下载成功。
- Chrome 对本次文件显示系统保存对话框，确认保存后下载完成；隐藏菜单里保留的旧进度文字不代表活动任务卡住。
- 抖音详情页实际 `playAddr` / 音轨 `urlList` 是 `{src}` 对象数组，原字符串假设导致误退到下载源。v0.2.2 修正后，MP4 与 DASH 对象地址回归测试均通过；同时按真实竖屏样本校正 B 站菜单的分辨率副标题。
- YouTube 回归：真实 `jNQXAC9IVRw` 菜单显示视频与音频选项，240p 直链下载得到 H.264 320×240 + AAC，18.9475 秒、629,172 字节。
- X 回归：真实 SpaceX `2099912172146995456` 菜单显示 2160p / 1080p / 720p / 360p / 270p，270p 直链下载得到 H.264 480×270 + AAC，53.9093 秒、744,740 字节。
- 本轮四个最终 MP4（B 站 P1/P2、YouTube、X）均经 `ffmpeg -v error -i <文件> -f null -` 完整解码，退出码为 0；文件保留在用户下载目录，未纳入仓库。
- v0.2.2 已在用户 Chrome 154 中确认生效。抖音详情样例 `7682473218057309455` 的真实菜单列出 2160P/1440P HEVC、1080P/720P/576P AVC，未再误退到平台下载源。
- 推荐流实际切换 `7678211585902726907` → `7678153321458191616`：旧菜单关闭，重新打开后标题、时长、媒体 ID 和画质均属于新作品；途中广告内容被拒绝解析。
- 从精选卡片打开作品弹层，URL 带 `modal_id=7685063012075572532`，清晰度菜单正常；弹层内切换到 `7649735935034150198` 后旧菜单关闭，重开显示新作品的标题、时长和媒体 ID。
- **v0.2.2 曾失败，v0.2.3 已修复：抖音完整 MP4 下载。** 原先点击详情样例的 576P 后，Chrome 下载项变为 `.html` 并显示“无法从网站上提取文件”，下载目录没有最终视频。对同一媒体地址设置 `cache: 'no-store'` 做对照：带页面 Referer 时普通 GET 为 200、Range GET 为 206，均为 `video/mp4`；去掉 Referer 后两者均为 403、`text/html`。未将页面内探测请求作为扩展下载成功的证据。
- 调用链核查：扩展后台使用 Service Worker 调用 `chrome.downloads.download`；[Chromium Downloads API](https://github.com/chromium/chromium/blob/main/chrome/browser/extensions/api/downloads/downloads_api.cc) 为无关联 frame 的调用构造下载参数，而 [DownloadManager 的 URLLoaderFactory](https://github.com/chromium/chromium/blob/main/content/browser/download/download_manager_impl.cc) 仅在有关联 frame 时接入扩展请求修改流程。因此当前来源规则不能保证后台原生直链下载带上 Referer；此结论由源码与上述真实失败共同支持，未直接抓取 Chrome 原生下载请求头。
- **v0.2.3 已实施用户确认的调整**：两站需要 Referer 的完整 MP4 以 `fetch` 模式复用已有 offscreen 分段拉取、OPFS 原样保存和完成交接，不重新封装；复用进度、取消和单任务槽位。菜单显示“下载中”，双轨仍显示“合并中”。保持扩展自身 initiator 与指定 CDN 限制，不增加权限或生产依赖；YouTube/X 原生直链路径保留。
- 新增 `tests/file-download.test.ts` 使用实际后台/离屏构建，模拟 CDN 对原生下载返回 `SERVER_FORBIDDEN`。修复前 5 项测试失败；修复后覆盖两站跨 8 MiB 分段的最终 Blob 与输入逐字节一致、文件名保留、403 不保存错误页且错误回传后可重试、未知长度保存、取消中断网络并清理临时文件、完整文件与合并共用槽位，以及 YouTube/X 直链仍可并行。网络与 OPFS 为匿名内存模拟，不能代替真实 Chrome CDN 验收。
- **v0.2.3 真实详情页下载验证完成，推荐流最终下载当时未覆盖**：用户更新后刷新抖音详情页，菜单显示 v0.2.3，各画质采用 `fetch`；此前失败的 576P 文件成功保存。随后启动 2160P 下载，实际点击“点击取消下载”后菜单恢复可选；重新下载同一选项成功保存 4K 文件。
- 抖音 576P 文件抽查第 1、138、275 秒画面，未见抖音平台水印，原画面中的台标和创作者标记保留。此为播放源样本的画面抽查，不代表所有作品均无水印；平台下载源的“可能带平台水印”提示由自动化测试覆盖。
- B 站 v0.2.3 真实错误恢复：仅在测试标签页临时阻断 `/x/web-interface/view`，菜单显示请求失败与“重试”；恢复请求后点击“重试”，重新显示 P2 正确标题、媒体 ID 与三个可用画质。阻断规则已清空。选择 720P 后观察到合并进度，最终 MP4 保存成功。
- 下列三个 v0.2.3 最终文件均经 `ffprobe` 检查和 `ffmpeg -v error -i <文件> -f null -` 完整解码，退出码均为 0；音频音量检测确认非全静音。文件保留在用户下载目录，画面抽查文件位于本地忽略目录 `output/validation-0.2.3/`，均未纳入扩展构建。

| v0.2.3 实际下载 | 视频轨 | 音频轨 | 文件时长 | 大小 |
| --- | --- | --- | --- | --- |
| 抖音 `7682473218057309455` / 576P | H.264 1024×576 | AAC | 277.0000 秒 | 31,644,846 字节 |
| 同一抖音作品 / 2160P 60 帧 | HEVC 3840×2160、60 fps | AAC | 276.9667 秒 | 85,790,743 字节 |
| B 站 `BV1n54y1t7hU` / P2 / 720P 准高清 | H.264 624×1280 | AAC | 15.1624 秒 | 2,447,866 字节 |

### 完成标准核对与边界

- 已验证两站普通视频的清晰度菜单、最终 MP4 音视频、抖音推荐流/详情/弹层目标切换与 B 站当前分 P；页面切换验证的版本分别为 v0.2.2 与 v0.2.1，相应定位代码在 v0.2.3 未改变。
- 已验证抖音完整文件取消后重新下载、B 站合并取消后重新下载，以及真实解析失败后重试；CDN 403 的错误回传、临时文件清理及重试成功由实际构建的自动化测试覆盖。
- YouTube/X 原生直链已完成前述真实下载和完整解码；v0.2.3 对两者的共享路由、忙碌时直链下载和原有解析运行了自动化回归。
- 实现沿用原生产依赖；新平台 Referer 规则限定扩展自身及两站指定媒体域名，权限与域名约束有自动化测试。
- 不在本次范围内：批量/整合集下载、直播、图集、番剧、绕过账号权限、转码、裁剪或 AI 去水印。画质仅取当前账号实际可用媒体；未遍历全部作品、会员状态或编码组合，B 站单轨 MP4 与抖音 DASH 分支主要由自动化样本覆盖。

以下保留 v0.1.0 的历史验证，不作为 v0.2.3 的真实下载证明。

日期：2026-09-16（Asia/Shanghai）

## 构建与自动化测试

- `npm ci --ignore-scripts`：依赖安装成功，沿用 YtDown 锁定的依赖版本。
- `npm test`：TypeScript 检查、四入口构建完成；7 个测试文件、53 项测试全部通过。
- 覆盖：YouTube 格式筛选与音轨配对、X 响应解析和多视频、文件名、平台结果转换、域名白名单、后台解析错误、直链下载、合并失败回传、取消释放、忙碌时直链下载、同时两个合并请求只接受一个，以及构建资源和权限。
- 最终构建为 `dist/`，约 468 KB，无右键菜单权限。

## Chrome 真实站点验证

在独立的 Chrome for Testing 149.0.7827.55 配置中加载实际 `dist/`，未更改用户日常 Chrome 配置。

| 项目 | 结果 |
| --- | --- |
| YouTube 普通页面悬停和菜单 | 实际页面显示 VideoDown 按钮、清晰度与音频选项 |
| YouTube 直链下载 | `Me at the zoo` 视频下载完成，629,172 字节；ffprobe 确认 H.264 320×240 + AAC，时长约 18.95 秒 |
| YouTube 高清选项解析 | 另一真实页面成功显示 2160p、1440p、1080p、720p、480p 合并选项及音频选项 |
| X 页面定位和 FxTwitter 解析 | 实际公开推文显示 2160p、1080p、720p、360p、270p |
| X 直链下载 | 270p 下载完成，1,186,816 字节；ffprobe 确认 H.264 480×270 + AAC，时长约 74.84 秒 |

浏览器测试使用 CDP 指定测试下载目录，该模式可能覆盖最终落盘文件名；平台命名规则通过独立自动化测试验证。

## Chrome 合成数据验证

仍使用真实扩展构建与 Chrome，模拟页面/API/媒体输入，避免依赖外站状态：

- 实际 YouTube MAIN 桥接 → 格式转换 → 公共菜单 → Service Worker → offscreen → OPFS → mediabunny → Chrome 下载完整链路通过。
- 使用 FFmpeg 生成两秒纯色视频和正弦音频；合并产物 21,419 字节，ffprobe 确认 H.264 320×180 + AAC，时长 2.00 秒，完成后 `busy` 为空。菜单中的 1080p 为合成 API 标签，不代表测试视频的真实像素尺寸。
- 延迟合成媒体读取后，在全屏菜单显示「合并中 0%」及取消入口；点击取消后活动条目恢复、`busy` 清空，退出全屏后宿主回到页面根节点。
- Shorts 合成页面验证活动 reel 的定位：视频元素纵向偏移到容器外时，按钮仍以容器顶部和画面水平范围定位，处于可见状态。
- X 合成双视频响应在同一菜单中分别显示「视频 1 / 720p」和「视频 2 / 360p」，Escape 能关闭菜单。

模拟测试最初受 YouTube 的站点 Service Worker 干扰；改用独立配置阻止站点 Service Worker。离屏页面的媒体读取用测试会话内的 fetch 替身提供合成字节；正式源码和构建中均不包含这些替身。

测试截图、脚本和下载样本保留在本地忽略目录 `output/playwright/`，未纳入扩展构建。

## 验证边界

- 真实站点完成了直链下载；高清合并以合成双轨验证，未下载真实 4K 长视频。
- WebM 格式筛选和音轨配对经过自动化测试，本轮未验证真实 WebM 成片。
- 未遍历所有登录状态、受保护推文和页面布局；X 仍依赖 FxTwitter，YouTube 仍沿用原解析接口。
- 未承诺 Edge、Firefox、Safari、商店发布、队列或动态平台扩展包。

## 手工复验

1. 加载 `dist/`，刷新两平台页面，悬停视频并下载低清晰度样本。
2. 在有双轨选项的 YouTube 页面选择高清格式，检查进度、取消、重开任务及最终音画。
3. 在另一标签页同时发起合并，确认忙碌提示；同时可继续发起直链下载。
4. 验证 Shorts、全屏、页面导航以及 X 多视频推文的定位和菜单分组。
5. `npm test` 可重新执行全部自动化验证。
